# xiao27 设计规范

> 这份规范适用于 **xiao27 名下所有个人网站与作品**（xiao27.com、nce.xiao27.com、
> shadowing.xiao27.com 以及以后新建的项目）。新项目开工前先读这一页。
>
> 唯一真源在 `xiao27-hub/brand/`，其他项目用 `npm run sync:brand` 同步，
> 不要在子项目里直接改 `brand.css`。

## 一、主题：只有三种，必须全支持

每个站都必须提供且只提供这三种主题：

| 主题 | 说明 | 实现 |
| --- | --- | --- |
| 浅色 | 近白底、黑字 | `<html data-theme="light">` |
| 深色 | 近黑底、白字 | `<html data-theme="dark">` |
| 跟随系统 | 默认值，跟随 `prefers-color-scheme` | `<html>` 不带 `data-theme` |

约定：

- 选择存在 `localStorage` 的 `xiao27-theme` 键，三个站共用同一个键名。
- 首屏渲染前必须在 `index.html` 里用内联脚本套用已存主题，避免主题闪烁。
- 顶栏放统一的三档切换控件（太阳 / 月亮 / 显示器）。

## 二、配色：黑白灰占九成，彩色只做点缀

**基调是黑白灰**，这是"高级感"的来源。彩色允许出现，但必须满足两个条件：

1. **面积小**：图标、文字、细描边、小圆点、进度条。不做大面积填充背景。
2. **有含义**：颜色必须在传达状态，而不是在装饰。

已定义的功能色（只有这三个，不要自造）：

| 变量 | 含义 | 典型用法 |
| --- | --- | --- |
| `--color-accent`（琥珀） | 此刻正在发生的事 | 当前朗读的句子 / 单词 |
| `--color-success`（绿） | 完成、正确、在线 | 学完标记、答对提示、在线状态点 |
| `--color-danger`（红） | 错误、破坏性操作 | 答错提示、删除确认 |

**反面例子**（都是踩过的坑）：把琥珀色当主色刷满整站；用绿色填满整块面板；
用蓝色渐变做主按钮。主按钮永远是 `.btn-primary`——浅色主题黑底白字，
深色主题白底黑字，不用彩色。

## 三、颜色不许写死

组件里禁止出现 `bg-zinc-900`、`text-white`、`#09090b` 这类固定色值，
否则主题切换不会生效。一律用语义类：

| 用途 | 类名 |
| --- | --- |
| 页面底色 | `bg-base` |
| 卡片 / 浮层 | `bg-raised`、或直接用 `.surface` / `.glass-card` |
| 悬停底色 | `bg-hovered` |
| 主要文字 | `text-ink` |
| 次要文字 | `text-ink-soft` |
| 弱化文字 | `text-ink-mute` |
| 描边 | `border-line`、`border-line-strong` |
| 主按钮 | `.btn-primary`（或 `bg-btn` + `text-btn-fg`） |
| 次按钮 | `.btn-secondary` |
| 选中 / 激活态 | `.is-active` |
| 弹窗遮罩 | `.overlay` |

例外（这些本来就该固定，不随主题变）：视频播放区的黑底、
压在图片上的渐变遮罩与其上的白字、二维码的白底、品牌标的黑色底座。

## 四、圆角：只有三档

| 档位 | 值 | 用在哪 |
| --- | --- | --- |
| `rounded-md` | 6px | 按钮、输入框、图标按钮 |
| `rounded-xl` | 12px | 卡片、弹窗、面板 |
| `rounded-full` | 胶囊 | 标签、头像、状态点 |

不要用 `rounded-sm` / `lg` / `2xl` / `3xl`。

## 五、字体

- 正文：Inter（中文回落到苹方 / 微软雅黑）
- 代码与数字标签：JetBrains Mono
- 衬线展示字体 `--font-display`：仅 visual-nce 的英文标题使用，别的站不要用

## 六、站点外壳

每个站顶栏左上角都是同一个品牌标：**黑色圆角方块 + 站点图标 + 站名 + `/ xiao27`**，
点击回到 xiao27.com。站点图标各不相同，但画法统一：黑底圆角方块 + 白色棱面图形
（hub 是凤凰，NCE 是摊开的书，shadowing 是播放三角与它的影子）。

新站上线前的自检：

- [ ] 三种主题都切一遍，没有看不见的文字或消失的图标
- [ ] 组件里 `grep -E '(bg|text|border)-(zinc|white|black)'` 只剩上面列的例外
- [ ] 圆角只有三档
- [ ] 顶栏有品牌标、能点回 hub、有主题切换
- [ ] 站点图标是同族新图形，不与已有的重复
